# sglang 0.5.11+ ships an aarch64 manylinux wheel on PyPI whose Requires-Dist
# pins torch==2.11.0 / torchaudio==2.11.0, locking an ABI-consistent set with
# the cu130 torch wheel installed above. The [all] extra is safe on aarch64
# now that sglang switched from `decord` to `decord2` for the aarch64/arm64
# transitive dep (decord2 ships cp312 aarch64 wheels; legacy `decord` did not).
# 0.5.11 is the floor for Gemma 4 support (sgl-project/sglang#21952).
sglang[all]>=0.5.11
